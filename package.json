{
  "scripts": {
    "build": "",
    "install-all": "npm install && cd client && npm install",
    "build-all": "npm run build && cd client && npm run build",
    "install-build": "npm run install-all && npm run build-all"
  },
  "dependencies": {
    "axios": "^1.3.5",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "js-yaml": "^4.1.0",
    "nodemon": "^2.0.22",
    "winston": "^3.8.2"
  }
}